# Write a Python program to convert a list of characters into a string. 

list1=['id','name','sub']

string1= " ".join(list1)    # join method used to convert list into string to concatenate the characters into a string. The empty string '' is used as the delimiter between characters.

print(string1)